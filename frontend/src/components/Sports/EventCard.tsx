import Link from 'next/link'; import { MarketGroup } from './MarketGroup';
export const EventCard = ({ event }: any) => <article className="bg-[#101b27] border border-white/5 rounded-2xl p-5"><div className="flex justify-between"><div><span className="text-[10px] uppercase text-blue-400">{event.sport.replaceAll('_', ' ')}</span><h3 className="font-bold text-lg">{event.name}</h3><time className="text-xs text-gray-500">{new Date(event.startsAt).toLocaleString()}</time></div><Link className="text-sm text-blue-400" href={`/sports/event/${event.eventId}`}>All markets →</Link></div>{event.markets.slice(0, 2).map((market: any) => <MarketGroup key={market.marketId} event={event} market={market} />)}</article>;
