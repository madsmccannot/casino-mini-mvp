export const LiveIndicator = ({ connected }: { connected: boolean }) => <span className={`inline-flex items-center gap-2 text-xs font-bold ${connected ? 'text-emerald-400' : 'text-red-400'}`}><i className={`w-2 h-2 rounded-full ${connected ? 'bg-emerald-400 animate-pulse' : 'bg-red-400'}`} />{connected ? 'ODDS LIVE' : 'FEED OFFLINE'}</span>;
