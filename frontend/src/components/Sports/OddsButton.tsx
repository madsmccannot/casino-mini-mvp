import { useBetSlipStore } from '../../state/betSlipStore';
export const OddsButton = ({ event, market, selection }: any) => {
  const { legs, add, remove } = useBetSlipStore(); const selected = legs.some(leg => leg.selectionId === selection.selectionId);
  const disabled = market.status !== 'ACTIVE' || selection.status !== 'ACTIVE';
  return <button disabled={disabled} onClick={() => selected ? remove(selection.selectionId) : add({ selectionId: selection.selectionId, selectionName: selection.participant ? `${selection.participant}: ${selection.name}` : selection.name, eventId: event.eventId, eventName: event.name, marketId: market.marketId, marketName: market.name, displayedMarketVersion: market.version, displayedOddsMillionths: selection.oddsMillionths })} className={`rounded-lg p-3 text-left border ${selected ? 'bg-blue-600 border-blue-400' : 'bg-[#172533] border-white/5 hover:border-blue-500/50'} disabled:opacity-40`}>{selection.boostId && <span className="float-right text-[9px] bg-amber-500 text-black rounded px-1 font-black">{selection.boostLabel || 'BOOST'}</span>}<span className="block text-xs text-gray-300">{selection.participant && <b>{selection.participant} · </b>}{selection.name}</span>{selection.originalOddsMillionths && <del className="text-[10px] text-gray-500 mr-2">{(Number(selection.originalOddsMillionths) / 1e6).toFixed(2)}</del>}<strong>{(Number(selection.oddsMillionths) / 1e6).toFixed(2)}</strong></button>;
};
